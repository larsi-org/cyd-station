// Copy this file to secrets.h (gitignored) and fill in real values.

#pragma once

#define WIFI_SSID     "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// Station to display -- sent as the API's `prefix` query param. See
// https://larsi.org/weather/ for the station list (ICAO codes, e.g. "KABQ").
#define STATION_PREFIX "KABQ"
